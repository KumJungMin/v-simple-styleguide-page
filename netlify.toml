[build]
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm --dir apps/styleguide-demo build"
  publish = "apps/styleguide-demo/dist"

[build.environment]
  NODE_VERSION = "18"
  PNPM_VERSION = "9.0.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 
